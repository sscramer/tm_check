{
  "name": "technomix-check",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check.js",
    "check:force": "node scripts/check.js --force",
    "publish": "node scripts/publish.js",
    "publish:force": "node scripts/publish.js --force",
    "validate": "node scripts/validate.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {}
}
